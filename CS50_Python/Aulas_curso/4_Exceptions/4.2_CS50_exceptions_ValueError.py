x = int(input("What's x? "))
print(f"x is {x}")

# ValueError acontece quando o usuário digita algo que não 
# condiz com o tipo de dados esperado e aqui o que nós esperamos é 
# um número inteiro mas se o usuário digitar "gato", por exemplo,
# o programa irá gerar um ValueError